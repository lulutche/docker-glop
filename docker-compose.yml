version: '3.8'

services:
  mas_evenements:
    build:
      context: ../mas_evenements
      dockerfile: Dockerfile
    image: glopcoder/mas_evenements
    ports:
        - "8080:8080"
    container_name: mas_evenements
    environment:
      SPRING_DATASOURCE_URL: jdbc:oracle:thin:@oracle.fil.univ-lille.fr:1521:filora
      SPRING_DATASOURCE_USERNAME: GLOP41
      SPRING_DATASOURCE_PASSWORD: novembre22
